# LF in the repository, the platform's own endings in the working tree.
* text=auto

# The fallback build scripts are run by bash: CRLF makes it fail with "$'\r': command not found".
*.sh text eol=lf
